#!/bin/sh
set -e

dlUrl='https://raw.githubusercontent.com/edemko/env-cabal/master/env-cabal.bash'
installPath="$HOME/bin/env-cabal"

echo "[FILE] $installPath"
wget "$dlUrl" -O "$installPath"
chmod +x "$installPath"
